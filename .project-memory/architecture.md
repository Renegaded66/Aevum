# Architecture

Siehe `docs/MASTERPLAN.md` und `docs/ARCHITECTURE.md`.

Aktuelle Entscheidung: Kotlin + Jetpack Compose + Material 3 + MVVM/MVI + Hilt + Room + DataStore + Navigation Compose. Noch kein App-Code.
